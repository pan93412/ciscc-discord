/**
 * Build the service notification message.
 *
 * @param identifier The identifier of this service.
 * @param message The service message.
 */
export default function BuildNotification(identifier: string, message: string): string;
